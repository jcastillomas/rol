openapi: 3.1.1
info:
    title: rol sandbox
    version: 1.0.0
    description: Rol Open documentation & sandbox
servers:
    - url: http://localhost:8800
      description: Development environment
components:
    schemas:
        Rule:
            type: object
            properties:
                id:
                    $ref: '#/components/schemas/Id'
        Id:
            type: string
            example: 8ecf229b-fdc8-4794-9f17-a690ec680081
        Number:
            type: integer
            example: 0

tags:
    - name: Health
    - name: Rules
paths:
    ######################################
    ####            HEALTH            ####
    ######################################
    /health:
        get:
            tags:
                - Health
            summary: Health check
            responses:
                '200':
                    description: Service available
                    content:
                        application/json:
                            schema:
                                type: object

    ######################################
    ####            REULES            ####
    ######################################
    /rules:
        post:
            tags:
                - Rule
            summary: Insert a Rule.
            requestBody:
                content:
                    application/json:
                        schema:
                            type: object
                            properties:
                                id:
                                    $ref: '#/components/schemas/Id'
            responses:
                '201':
                    description: Resource Created
                    content:
                        application/json:
                            schema:
                                type: string
                                default: { }
                '400':
                    description: Bad Request
                '401':
                    description: Unauthorised
                '409':
                    description: Rule cannot be created
    /rules/{id}:
        get:
            tags:
                - Rule
            summary: Find a Rule.
            parameters:
                -   name: id
                    in: path
                    required: true
                    description: the rule identifier, as RuleId
                    schema:
                        $ref: '#/components/schemas/Id'
            responses:
                '200':
                    description: Resource Found
                    content:
                        application/json:
                            schema:
                                type: object
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Rule'
                                    metadata:
                                        type: object
